{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center mt-5">Plus de détails</h1>
<div class="container">
    <div class="row mt-5 text-center">
            <div class="col-12">
                <h4>{{ book.title}}</h4>
                <img src="{{ asset('images/' ~ book.cover )}}" alt="" class="w-60">
                <p>{{ book.resume }}</p>
                <p>Parution {{book.parution|date("d/m/Y") }}</p>
                <p>Prix : {{ book.prix }}</p>
            </div>
    </div>
</div>

{% endblock %}